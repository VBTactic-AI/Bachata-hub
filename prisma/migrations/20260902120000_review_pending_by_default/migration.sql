-- Отзывы теперь публикуются только после проверки модератором (было: сразу,
-- реактивная модерация). Меняем дефолт колонки — существующие отзывы (уже
-- APPROVED/REJECTED) не трогаем, это влияет только на новые записи.
ALTER TABLE "Review" ALTER COLUMN "moderationStatus" SET DEFAULT 'PENDING';
