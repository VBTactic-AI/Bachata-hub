-- Bachata HUB Belarus — слой 3
-- Финал: четвёртый формат — "Относительные места" (скейтинг-система),
-- CLAUDE.md §18/§24, docs/00_DECISIONS.md A22 (доп. запись A26/A27).
--
-- Судья ставит МЕСТО напрямую (1..N, без повторов в рамках роли), а не
-- баллы по критериям — итог считается отдельным ranking engine
-- (rankFinalParticipantsBySkatingSystem, final-ranking.ts), НЕ суммой и НЕ
-- средним. Схема данных не меняется: формат по-прежнему хранит своё единственное
-- "место"-значение как обычный FinalJudgeScore.value с одним синтетическим
-- FinalCriterion (например «Место», диапазон 1..N) — новых таблиц/колонок не
-- требуется, только новое значение перечисления.

ALTER TYPE "FinalFormat" ADD VALUE 'RELATIVE_PLACEMENT';
